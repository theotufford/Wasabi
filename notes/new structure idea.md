new structure for the custom forms / methods system: python wrappers that export their structure to the interface.
works the same as registering a flask endpoint.

needs to have a name
a well select type (these are premade and chosen by name)
then the rest of the args become fields in the instruction form

I really really need to have a little lcd and some control buttons and a dial probably

needs to be made async, probably with fastapi

I need to have a busy state for the coms

need to actually implement CRC32

also add a datatype header byte
